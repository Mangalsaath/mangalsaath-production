# Testing Guide – v1.6.0

1. Run `npm install` and `npm run dev`.
2. Open Discover and test keyword, city, religion, marital status and age filters.
3. Test Verified Profiles Only and all four sorting options.
4. Confirm result count and Previous/Next pagination controls.
5. Shortlist and remove profiles; refresh the browser and confirm shortlist persistence.
6. Log in with `vikas@example.com` / `123456` and confirm compatibility scores display.
7. Open My Shortlist and confirm profile, interest and message actions work.
8. Check the discovery interface on desktop and mobile widths.

Build verification: `npm run build` passed.
